mutation ClientUpdatePassword($currentPassword: String!, $newPassword: String!, $confirmPassword: String!) {
    clientUpdatePassword(currentPassword: $currentPassword, newPassword: $newPassword, confirmPassword: $confirmPassword) {
      success
      message
    }
  }