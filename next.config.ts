/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['tailwindui.com', 'images.unsplash.com'], // Add required image domains
  },
  // Add other config options here
};

module.exports = nextConfig;